spring:
  application:
    name: ai-japanese-speaking
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-4o-mini
      audio:
        transcription:
          options:
            model: gpt-4o-mini-transcribe
        speech:
          options:
            model: gpt-4o-mini-tts
            voice: alloy
            response-format: mp3

jwt:
  secret: ${JWT_SECRET}
  expiration-seconds: ${JWT_EXPIRATION_SECONDS:3600}
